<style>
  h3, table {
    text-align: center;
  }
  tr {
    font-size: 17px;
  }
  .fa {
    font-size: 17px;
  }
  #aufsicht > tbody > tr:nth-child(1) {
    background-color: #ebebeb;
  }
</style>

<table style="color: black;" class="table" id="aufsicht">
  <th><h3>Geänderte Aufsichten</h3></th>
</table>


<script type="text/javascript">
  console.log('/' + location.pathname.split('/')[1] + '/' + location.pathname.split('/')[2]);

  $.getJSON('/' + location.pathname.split('/')[1] + '/' + location.pathname.split('/')[2], function (data) {
    if (data == "NO_VPLAN_AVAILABLE") {
      // $.get("/placeholder", function (html) {
      //   $('body').html(html);
      // });
      return;
    }

    console.log(data);

    $.getJSON("/settings", function (settings) {
      console.log(settings);

      $.each(data.aufsicht, function (i, item) {
        $('<tr>').html($('<td>').html(item.info.replace("-->", "<i class='fa fa-fw fa-lg fa-arrow-right'></i>"))).appendTo('#aufsicht'); 
      });
    });
  });

</script>

{{> reloadSocket}}